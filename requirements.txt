# Trading Bot Dependencies
# Python 3.9+ compatible versions (pinned for production stability)
# Only direct dependencies - pip will resolve transitive dependencies automatically
# Last verified: 2026-01-27

# Broker API
smartapi-python==1.5.5
logzero==1.7.0  # Required by smartapi-python (transitive dependency)
websocket==0.2.1  # Required by smartapi-python for WebSocket connections
gevent==23.9.1  # Required by websocket (Python 3.9 compatible)
greenlet==3.0.3  # Required by websocket (Python 3.9 compatible)

# Authentication
pyotp==2.9.0

# Configuration
python-dotenv==1.0.0

# Data Processing (Python 3.9 compatible)
# pandas 3.0.0+ requires Python 3.10+, so using pandas 2.x for Python 3.9
pandas==2.2.0
numpy==1.26.4

# Market Data
yfinance==0.2.40

# HTTP Requests
requests==2.31.0

# Web Dashboard
flask==3.0.0

# Machine Learning
scikit-learn==1.4.0

# Timezone Support
pytz==2024.1
